# sgzhd_helper - 静态站模板

网站内容位于 `public/`。现有 workflow 会把 `public/` 上传并部署到 GitHub Pages。

快速使用：
- 编辑 `public/index.html`、`public/assets/` 与 `public/data/`。
- 将更改推送到 `main` 分支，Actions 会自动部署。
- 如需自定义域，请在 `public/CNAME` 中写入域名并推送。
